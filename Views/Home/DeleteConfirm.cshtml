@model Product

<div class="car">
    <div class="card-body">
        <p>
            @Model.Name isimli ürünü silmek istediğinize emin misini?
            <form action="" method="post">
                <input type="hidden" asp-for="@Model.Id">
                <button type="submit" class="btn btn-outline-danger">Evet</button>
                <a asp-controller="Home" asp-action="Index" class="btn btn-outline-primary">Hayır</a>
            </form>
        </p>
    </div>
</div>